import static org.junit.Assert.*;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.json.JsonTest;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import com.lucene.createindex.DirectoryWatcher.WatchDirectory;
import com.lucene.createindex.DirectoryWatcher.WriteIndex;

@RunWith(SpringRunner.class)
public class watchDiretoryTest {

 
    
    @Test
    public void contexLoads() throws Exception {
    /*	 WriteIndex directory=new WriteIndex();
        String s="success";
    	String result=directory.writeIndex();
    	assertEquals(s, result);*/
    }
}
